/******************************************************************************/
/** git/github ******************************************************* start **/

set windows environment variable
HOME = C:\Users\user

install git from "http://code.google.com/p/msysgit/downloads/list" follow the 
instructions to generate SSH key and add it to github project

from gitbash:
cd /x/project directory
git init                    
git add *                   // add files to project     DO AFTER EVERY CHANGE
git commit -am "message"    // commit changes           BEFORE PUSH TO REPOSITORY

git remote add repository git://github.com/user/repository.git     
git push -u origin master

git pull

/* for pull add the following to ".git/config" if not there*/
[branch "master"]
	remote = origin
	merge = refs/heads/master

/** git/github ********************************************************* end **/
/******************************************************************************/