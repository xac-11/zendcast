git/github

set windows environment variable
HOME = C:\Users\user

install git from "http://code.google.com/p/msysgit/downloads/list" follow the instructions


from gitbash: change directory to project root

git init
git add *
git commit -am "message"

git remote add repository git://github.com/user/repository.git
git push -u origin master

git pull

/* for pull add the following to ".git/config" if not there*/
[branch "master"]
	remote = origin
	merge = refs/heads/master